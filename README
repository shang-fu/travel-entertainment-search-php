Create a webpage that allows you to search for places information using the Google Places API, and the results will be displayed in a tabular format. The page will also provide reviews and photos for the selected place.

Technology:
	* front-end: HTML5, CSS3, JavaScript
	* back-end: PHP
	* server: Apache as PHP interpreter, Nginx as proxy server

Objectives: 
	* get experience with the PHP programming language
	* get experience with the Google Places API
	* get experience using JSON parsers in PHP and JavaScript
	* be familiar with AJAX